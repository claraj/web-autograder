def grade(project_location):

    # assumes there's a grades/config.json file in the root dir of project that
    # lists where the test report files are
    # And there is a grades.json file in /grades directory.  (  "report_locations": )
    pass
